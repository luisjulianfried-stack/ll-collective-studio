const menuButton=document.querySelector('.menu-button');
const nav=document.querySelector('#navigation');
menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')!=='true';menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Menü schließen':'Menü öffnen');nav.classList.toggle('open',open);menuButton.querySelector('span').textContent=open?'−':'+'});
function closeMenu(){menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Menü öffnen');nav.classList.remove('open');menuButton.querySelector('span').textContent='+'}
nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});
const tabs=[...document.querySelectorAll('[data-category]')];
function selectCategory(category){const data=window.CAFE[category];tabs.forEach(t=>{const active=t.dataset.category===category;t.setAttribute('aria-selected',String(active));t.tabIndex=active?0:-1});const panel=document.querySelector('#menu-panel');panel.setAttribute('aria-labelledby','tab-'+category);panel.replaceChildren();data.items.forEach(([name,description,price])=>{const row=document.createElement('div');row.className='menu-row';const body=document.createElement('div');const heading=document.createElement('h3');heading.textContent=name;const desc=document.createElement('p');desc.textContent=description;body.append(heading,desc);const amount=document.createElement('span');amount.className='price';amount.textContent=price+' €';row.append(body,amount);panel.append(row)});const note=document.createElement('p');note.className='category-note';note.textContent=data.note;panel.append(note);const img=document.querySelector('#menu-image');img.src='assets/'+data.image;img.alt=data.alt;document.querySelector('#menu-image-label').textContent=data.caption}
tabs.forEach((tab,i)=>{tab.addEventListener('click',()=>selectCategory(tab.dataset.category));tab.addEventListener('keydown',e=>{let index;if(e.key==='ArrowRight')index=(i+1)%tabs.length;if(e.key==='ArrowLeft')index=(i+tabs.length-1)%tabs.length;if(e.key==='Home')index=0;if(e.key==='End')index=tabs.length-1;if(index!==undefined){e.preventDefault();tabs[index].focus();selectCategory(tabs[index].dataset.category)}})});selectCategory('kaffee');
const dialog=document.querySelector('dialog');document.querySelector('.legal-trigger').addEventListener('click',()=>dialog.showModal());document.querySelector('.dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
const sections=[...document.querySelectorAll('section[id]')];const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){nav.querySelectorAll('a').forEach(a=>{if(a.hash==='#'+entry.target.id)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})}})},{rootMargin:'-20% 0px -50% 0px'});sections.forEach(s=>observer.observe(s));
window.addEventListener('scroll',()=>document.querySelector('.header').classList.toggle('scrolled',window.scrollY>30),{passive:true});
