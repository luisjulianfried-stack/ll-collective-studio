const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('#navigation');
function setMenu(open){menu.setAttribute('aria-expanded',String(open));menu.querySelector('span').textContent=open?'−':'+';nav.classList.toggle('open',open)}
menu.addEventListener('click',()=>setMenu(menu.getAttribute('aria-expanded')!=='true'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});
const tabs=[...document.querySelectorAll('[data-category]')],panel=document.querySelector('#dishes');
function select(category){const data=window.RESTAURANT[category];tabs.forEach(t=>{const active=t.dataset.category===category;t.setAttribute('aria-selected',String(active));t.tabIndex=active?0:-1});panel.setAttribute('aria-labelledby','tab-'+category);panel.replaceChildren();const intro=document.createElement('p');intro.className='category-intro';intro.textContent=data.intro;panel.append(intro);data.items.forEach(([name,description,price])=>{const row=document.createElement('div');row.className='dish';const body=document.createElement('div');const h=document.createElement('h3');h.textContent=name;const p=document.createElement('p');p.textContent=description;body.append(h,p);const amount=document.createElement('span');amount.className='price';amount.textContent=price+' €';row.append(body,amount);panel.append(row)})}
tabs.forEach((t,i)=>{t.addEventListener('click',()=>select(t.dataset.category));t.addEventListener('keydown',e=>{let n;if(['ArrowDown','ArrowRight'].includes(e.key))n=(i+1)%tabs.length;if(['ArrowUp','ArrowLeft'].includes(e.key))n=(i+tabs.length-1)%tabs.length;if(e.key==='Home')n=0;if(e.key==='End')n=tabs.length-1;if(n!==undefined){e.preventDefault();tabs[n].focus();select(tabs[n].dataset.category)}})});select('antipasti');
const dialog=document.querySelector('#legal');document.querySelector('#legal-open').addEventListener('click',()=>dialog.showModal());document.querySelector('#legal-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)nav.querySelectorAll('a').forEach(a=>{if(a.hash==='#'+e.target.id)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})})},{rootMargin:'-20% 0px -50% 0px'});document.querySelectorAll('section[id]').forEach(s=>observer.observe(s));
